{
  "name": "grid",
  "version": "2.0.0",
  "description": "JSONGrid - Convert JSON data to an interactive grid for visualization",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@vitest/coverage-v8": "^3.0.4",
    "@vitest/ui": "^3.0.4",
    "jsdom": "^26.0.0",
    "vitest": "^3.0.4"
  }
}
